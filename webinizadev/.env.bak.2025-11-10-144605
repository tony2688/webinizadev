APP_NAME="WebinizaDev"
APP_ENV=production
APP_KEY=base64:8DjzHxYsUIS2CcdPQJ5Sv63rffuwiywc9i2orNgOccs=
APP_DEBUG=false
APP_URL=https://webinizadev.com

APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_AR

LOG_CHANNEL=stack
LOG_LEVEL=info

# ? Evita errores buscando DB: se usa driver array
DB_CONNECTION=array

# ? Sesiones sin DB
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

# ? Cache sin DB
CACHE_STORE=file

# ? Cola sin DB
QUEUE_CONNECTION=sync

APP_DEBUG=true
LOG_CHANNEL=stack
LOG_LEVEL=debug

# ? Disco local
FILESYSTEM_DISK=local

# ? Correo DonWeb / Ferozo
MAIL_MAILER=smtp
MAIL_HOST=c2162521.ferozo.com
MAIL_PORT=465
MAIL_USERNAME=info@webinizadev.com
MAIL_PASSWORD=@g0VCT26xQ
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS=info@webinizadev.com
MAIL_FROM_NAME="WebinizaDev"

# ?? Servicios desactivados (no usados)
REDIS_CLIENT=null
MEMCACHED_HOST=127.0.0.1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

VITE_APP_NAME="WebinizaDev"

# === IA vía Cloudflare (tu API propia) ===
AI_BASE_URL=https://api.webinizadev.com
AI_PATH=/v1/generate
AI_MODEL=qwen2.5:7b-instruct-q4_K_M

AI_API_KEY="Vnh36h7y!"
AI_TENANT_ID="cliente1"

# Producción: en lo posible true. Usar false solo si tu SSL local da problemas.
AI_SSL_VERIFY=true

# Identidad y estilo (tono humano, cercano y útil)
AI_SYSTEM="Sos el asistente oficial de WebinizaDev. Respondé ÚNICAMENTE con la información de WebinizaDev. Tono: cercano, humano y proactivo (vos + español de Argentina), frases cortas y claras; cuando sirva, usá listas de 3–6 ítems, y cerrá con una pregunta útil (CTA). Si te preguntan si sos una IA, decí que sí y que fuiste desarrollada por WebinizaDev. Si la consulta no corresponde al dominio, devolvé exactamente el refusal_message. No inventes ni cites marcas ajenas."

# RAG / Búsqueda
AI_KB=kb-webinizadev-v3
AI_TOP_K=7
AI_MIN_SCORE=0.10
AI_FALLBACK=refuse
AI_REFUSAL_MESSAGE="Solo puedo responder sobre WebinizaDev. ¿Querés consultar servicios, planes o contacto?"

# Guard server-side (bloqueo de dominio)
AI_STRICT_DOMAIN=true
AI_ALLOWED_TERMS="webinizadev, webiniza, servicios, plan, planes, landing, 
landingpage, portfolio, institucional, seo, marketing, seo marketing, ecommerce, e-commerce, 
tienda, dashboard, ia, chatbot, n8n, automatizacion, automatización, sitio, página, web, 
desarrollo web, diseño web, hosting, mantenimiento, cotización, presupuesto, precios, proceso, 
plazo, soporte, contacto, whatsapp, tucumán, tucuman, gabriela bollati, antonio romero, 
matias giacobbe, uv energia solar, sdt audio visual"

# Tuning
AI_TEMP=0.3
AI_MAX_TOKENS=450
AI_TIMEOUT=45

# Cloudflare Access (si protegiste el subdominio)
CF_ACCESS_CLIENT_ID=
CF_ACCESS_CLIENT_SECRET=
